export interface Book {
    id?: string;
    userId: string;
    hotelId: string;
    name: string,
    phone: string,
    startDate: Date;
    endDate: Date;
    totalPrice: number;
    hotelName: string;
  }
